<% if !@broadcast.content.embeds.empty? %>
    <%= image_tag @broadcast.content.embeds.find{|embeds| embeds.image?}.variant(resize_to_limit: [200, 200]), class: "preview_image" %>
<% else %>
    <%= image_tag "publication_card_placeholder", class: "preview_image", alt:"Preview Image" %>
<% end %>
